package program_9;

public class FractionsTest {
	
	public static void main(String args[]) { 
		
		// test
		Fraction f1 = new Fraction(1, 10); 
		Fraction f2 = new Fraction(2, 10); 
		
		Fraction fAdd = f1.add(f2); 
		Fraction fSubtract = f1.subtract(f2); 
		Fraction fMultiply = f1.multiply(f2); 
		Fraction fDivide = f1.divide(f2);
		
		//outputFractionResults()
	}
	
}
